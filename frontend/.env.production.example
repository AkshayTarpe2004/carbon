# Production build — copy to .env.production before `npm run build`
# Or set REACT_APP_API_URL in CI (e.g. GitHub Actions secrets → env for build step).
# Backend origin only (no trailing slash, no /api path).

REACT_APP_API_URL=https://deployment-r6qc.onrender.com
