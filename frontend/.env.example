# Local development — copy to .env (do not commit .env)
# Create React App only exposes variables prefixed with REACT_APP_

# Backend API (no trailing slash). Must match your Spring Boot URL and CORS config.
REACT_APP_API_URL=http://localhost:8080
